---
description: how to build the Next.js application
---
# Build Project Workflow

This workflow describes how to install dependencies and build the exported Next.js project.

// turbo-all
1. Install project dependencies using `npm`.
   `npm install`

2. Compile and build the Next.js project.
   `npm run build`
